// const fixed value.
const text = "const new";

// text = "const change"; 
// you uncommit 4th line showing compiling error.

console.log(text);

// user get collect project.

   //Don't using scanner. it is very bad way.
   var Name = "tagore";
   var age = 17;
   var study = "10 class";
   var rollNo = 12;
   var school = "Sri venu vidyalayam high school";

   console.log(`
   user name : ${Name}
   user age : ${age}
   user study : ${study}
   user rollNo : ${rollNo}
   user school : ${school}
   `);